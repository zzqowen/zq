import {
  createElement,
  appendNode,
  removeNode,
  domView,
  setClassList
} from "tools/utils/dom";
import genId from "tools/hash/hashSum";

let index = 0;

let swiper = (msg, durtion) => {
  
}

export default swiper;